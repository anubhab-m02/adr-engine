---
date: "2026-09-26"
verdicts:
  - pr_number: 205
    verdict: approve
    reason: "Docs-only change (docs/UI-DESIGN.md), no sensitive paths touched (no backend/auth/, config_store.py, workflows, migrations, or search.py), so no sensitive-path hold applies.\n\nThe rewrite accurately reflects shipped behavior. I cross-checked several specific claims against the actual code in frontend/src/ask/: the banner dismissal correctly uses `sessionStorage` (matching the doc's explicit \"not carried across sessions\" note, confirmed by a code comment in SourceCard/SourcesView.jsx), the print stylesheet forcing the <900px inline-collapse layout on paper is implemented in index.css with a comment tying it to the \"Track B spec,\" and all the named components (AnswerPage.jsx, PrivacyPanel.jsx, SourceCardList.jsx, CitationMarker.jsx) exist. The cross-referenced design doc (docs/superpowers/specs/2026-08-04-v2-design.md, \"Track B · Reading surfaces\" section) exists and its ASCII layout matches the margin-citation grid description. Git history confirms PR #167 shipped the margin-citation grid feature this doc now documents.\n\nThe PR stays within the linked issue's (#173) scope, correctly spins off an unrelated width-cap finding as a separate issue (#204) rather than fixing it inline, and follows the repo's \"one issue per PR\" convention."
  - pr_number: 206
    verdict: approve
    reason: "No sensitive path touched: the diff is confined to frontend/src (App.jsx, api.js, AskPage.jsx, RepoDecisionsPage.jsx, RepoRow.jsx, and new FileTreeView.jsx plus their tests) — none of backend/auth/, backend/config_store.py, .github/workflows/*, backend/retrieval/search.py, or anything migration-shaped is touched, so the sensitive-path hold does not apply here.\n\nOn the merits: this closes out prior work (#143/#144/#145) that was built but never wired up. I verified the pieces actually connect — FileTree.jsx already calls `navigate('/', { state: { prefillQuestion } })`, App.jsx's `/` route renders AskPage, and AskPage now reads `location.state?.prefillQuestion` and clears it via `navigate(location.pathname, { replace: true, state: null })`, so the flow is coherent end to end. The new `/library/:repo/files` route and `RepoDecisionsPage` VIEWS entry follow the exact pattern of the existing timeline/graph routes, and `FileTreeView.jsx`'s loading/error/empty states match `DecisionTimeline.jsx`'s conventions verbatim. `GET /decisions/by-path` already exists on the backend (routers/decisions.py) with its own tests, so no backend change was needed, matching the PR's claim. Stated scope matches the diff — no unrelated changes — and per CLAUDE.md's testing guidance for non-money-math frontend glue, the PR adds route/component/nav-state tests (FileTreeView.test.jsx, App.test.jsx, RepoRow.test.jsx, AskPage.test.jsx) covering the new behavior rather than relying on manual verification alone."
  - pr_number: 207
    verdict: approve
    reason: "No sensitive paths touched — this diff is entirely frontend (AskPage.jsx, its tests, App.test.jsx, CommandPalette.test.jsx, and a one-line comment update in GitHubSection.jsx). None of backend/auth/, backend/config_store.py, .github/workflows/*, migration-shaped files, or backend/retrieval/search.py are touched, so no sensitive-path hold applies.\n\nOn merits: this closes #201 and implements exactly the gap GitHubSection.jsx's own comment called out as deferred — docs/UI-DESIGN.md (line 269) explicitly states the expired-token state \"also triggers a global quiet banner on Ask.\" The implementation reuses the established pattern from SourcesView.jsx's degraded-mode banner (same bg-highlight styling, same sessionStorage-based per-session dismissal, same key-naming convention) rather than inventing new mechanics, and correctly fails closed (banner hidden) if the auth-status check errors. Test coverage is thorough: expired/authorized/failed-check/dismiss-persists-on-remount are all covered, and the three other test files that mount AskPage/App are updated to stub the new API call so nothing regresses. The PR explicitly avoids introducing a new shared auth-status context for a single consumer, keeping the change scoped to what the issue asked for."
findings: []
---

# Review report — 2026-09-26

## PRs reviewed

PR #205: **approve** — Docs-only change (docs/UI-DESIGN.md), no sensitive paths touched (no backend/auth/, config_store.py, workflows, migrations, or search.py), so no sensitive-path hold applies.

The rewrite accurately reflects shipped behavior. I cross-checked several specific claims against the actual code in frontend/src/ask/: the banner dismissal correctly uses `sessionStorage` (matching the doc's explicit "not carried across sessions" note, confirmed by a code comment in SourceCard/SourcesView.jsx), the print stylesheet forcing the <900px inline-collapse layout on paper is implemented in index.css with a comment tying it to the "Track B spec," and all the named components (AnswerPage.jsx, PrivacyPanel.jsx, SourceCardList.jsx, CitationMarker.jsx) exist. The cross-referenced design doc (docs/superpowers/specs/2026-08-04-v2-design.md, "Track B · Reading surfaces" section) exists and its ASCII layout matches the margin-citation grid description. Git history confirms PR #167 shipped the margin-citation grid feature this doc now documents.

The PR stays within the linked issue's (#173) scope, correctly spins off an unrelated width-cap finding as a separate issue (#204) rather than fixing it inline, and follows the repo's "one issue per PR" convention.

PR #206: **approve** — No sensitive path touched: the diff is confined to frontend/src (App.jsx, api.js, AskPage.jsx, RepoDecisionsPage.jsx, RepoRow.jsx, and new FileTreeView.jsx plus their tests) — none of backend/auth/, backend/config_store.py, .github/workflows/*, backend/retrieval/search.py, or anything migration-shaped is touched, so the sensitive-path hold does not apply here.

On the merits: this closes out prior work (#143/#144/#145) that was built but never wired up. I verified the pieces actually connect — FileTree.jsx already calls `navigate('/', { state: { prefillQuestion } })`, App.jsx's `/` route renders AskPage, and AskPage now reads `location.state?.prefillQuestion` and clears it via `navigate(location.pathname, { replace: true, state: null })`, so the flow is coherent end to end. The new `/library/:repo/files` route and `RepoDecisionsPage` VIEWS entry follow the exact pattern of the existing timeline/graph routes, and `FileTreeView.jsx`'s loading/error/empty states match `DecisionTimeline.jsx`'s conventions verbatim. `GET /decisions/by-path` already exists on the backend (routers/decisions.py) with its own tests, so no backend change was needed, matching the PR's claim. Stated scope matches the diff — no unrelated changes — and per CLAUDE.md's testing guidance for non-money-math frontend glue, the PR adds route/component/nav-state tests (FileTreeView.test.jsx, App.test.jsx, RepoRow.test.jsx, AskPage.test.jsx) covering the new behavior rather than relying on manual verification alone.

PR #207: **approve** — No sensitive paths touched — this diff is entirely frontend (AskPage.jsx, its tests, App.test.jsx, CommandPalette.test.jsx, and a one-line comment update in GitHubSection.jsx). None of backend/auth/, backend/config_store.py, .github/workflows/*, migration-shaped files, or backend/retrieval/search.py are touched, so no sensitive-path hold applies.

On merits: this closes #201 and implements exactly the gap GitHubSection.jsx's own comment called out as deferred — docs/UI-DESIGN.md (line 269) explicitly states the expired-token state "also triggers a global quiet banner on Ask." The implementation reuses the established pattern from SourcesView.jsx's degraded-mode banner (same bg-highlight styling, same sessionStorage-based per-session dismissal, same key-naming convention) rather than inventing new mechanics, and correctly fails closed (banner hidden) if the auth-status check errors. Test coverage is thorough: expired/authorized/failed-check/dismiss-persists-on-remount are all covered, and the three other test files that mount AskPage/App are updated to stub the new API call so nothing regresses. The PR explicitly avoids introducing a new shared auth-status context for a single consumer, keeping the change scoped to what the issue asked for.

## Findings not tied to a specific PR

None this run — per-PR reviews only surface a verdict today; see
the implementation plan's Task 5 note on this known gap.
