---
date: "2026-09-25"
verdicts:
  - pr_number: 195
    verdict: approve
    reason: "Scope check: diff touches only frontend/src/ask/AskPage.jsx, AskPage.test.jsx, frontend/src/components/MessageList.jsx, and MessageList.test.jsx. None of these fall under the sensitive-path denylist (backend/auth/, backend/config_store.py, .github/workflows/*, migrations, backend/retrieval/search.py), so no sensitive-path hold applies here.\n\nThe change itself is a narrow, well-scoped bug fix matching its stated purpose: AskPage.jsx's runQuery() was dropping sent_to_cloud/cloud_synthesis_fields from the query response, and MessageList.jsx's AssistantMessage wasn't forwarding those fields to AnswerPage — so the privacy panel always showed its defaults instead of the real synthesis state. I verified AnswerPage.jsx already accepts and renders sentToCloud/cloudSynthesisFields props (it was the only piece not receiving real data), and confirmed the backend (backend/models.py, backend/routers/query.py) genuinely returns sent_to_cloud/cloud_synthesis_fields with those exact names, so the wiring is correct and not fabricated.\n\nThis isn't a synthesis-endpoint or citation-pipeline change, so the CLAUDE.md citation-preservation requirement doesn't apply, but the PR still adds an end-to-end test (AskPage.test.jsx) plus a component-level test (MessageList.test.jsx) exercising the real data path, consistent with this repo's testing conventions. The change stays within its stated scope with no unrelated additions."
  - pr_number: 196
    verdict: approve
    reason: "Diff is a single-line dev-dependency bump in backend/requirements-dev.txt (pytest 8.3.4 → 9.0.3), addressing a CVE per the linked Dependabot alert. This does not touch any sensitive path (backend/auth/, backend/config_store.py, .github/workflows/*, migrations, or backend/retrieval/search.py), so the sensitive-path hold doesn't apply here. The PR description explains why the other two Dependabot targets (postcss, vitest/@vitest/mocker) needed no change — they were already patched by prior PRs — and states pytest's full suite (253 tests) passes unchanged under pytest 9, with no removed APIs/plugins in use. Scope is minimal and matches the linked issue; no unrelated changes introduced. This is a low-risk, well-documented dependency bump."
findings: []
---

# Review report — 2026-09-25

## PRs reviewed

PR #195: **approve** — Scope check: diff touches only frontend/src/ask/AskPage.jsx, AskPage.test.jsx, frontend/src/components/MessageList.jsx, and MessageList.test.jsx. None of these fall under the sensitive-path denylist (backend/auth/, backend/config_store.py, .github/workflows/*, migrations, backend/retrieval/search.py), so no sensitive-path hold applies here.

The change itself is a narrow, well-scoped bug fix matching its stated purpose: AskPage.jsx's runQuery() was dropping sent_to_cloud/cloud_synthesis_fields from the query response, and MessageList.jsx's AssistantMessage wasn't forwarding those fields to AnswerPage — so the privacy panel always showed its defaults instead of the real synthesis state. I verified AnswerPage.jsx already accepts and renders sentToCloud/cloudSynthesisFields props (it was the only piece not receiving real data), and confirmed the backend (backend/models.py, backend/routers/query.py) genuinely returns sent_to_cloud/cloud_synthesis_fields with those exact names, so the wiring is correct and not fabricated.

This isn't a synthesis-endpoint or citation-pipeline change, so the CLAUDE.md citation-preservation requirement doesn't apply, but the PR still adds an end-to-end test (AskPage.test.jsx) plus a component-level test (MessageList.test.jsx) exercising the real data path, consistent with this repo's testing conventions. The change stays within its stated scope with no unrelated additions.

PR #196: **approve** — Diff is a single-line dev-dependency bump in backend/requirements-dev.txt (pytest 8.3.4 → 9.0.3), addressing a CVE per the linked Dependabot alert. This does not touch any sensitive path (backend/auth/, backend/config_store.py, .github/workflows/*, migrations, or backend/retrieval/search.py), so the sensitive-path hold doesn't apply here. The PR description explains why the other two Dependabot targets (postcss, vitest/@vitest/mocker) needed no change — they were already patched by prior PRs — and states pytest's full suite (253 tests) passes unchanged under pytest 9, with no removed APIs/plugins in use. Scope is minimal and matches the linked issue; no unrelated changes introduced. This is a low-risk, well-documented dependency bump.

## Findings not tied to a specific PR

None this run — per-PR reviews only surface a verdict today; see
the implementation plan's Task 5 note on this known gap.
