name: Daily Agent

on:
  schedule:
    - cron: "0 4 * * *"   # daily; the reusable workflow no-ops if the backlog is empty
  workflow_dispatch: {}    # manual test trigger

jobs:
  run:
    uses: anubhab-m02/automation-kit/.github/workflows/daily-agent.yml@main
    secrets:
      CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
